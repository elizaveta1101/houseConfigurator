export enum MenuLinkPaths {
  throughElementsPath = '/throughs',
  projectFinishPath = '/projects',
  projectInvestPath = '/invests',
  housesFinishPath = '/houses',
  infoPollPath = '/info-poll',
  clientDataPath = '/clients',
  adminsSitePath = '/admins',
  profilePath = '/profile',
  ordersPath = '/orders',
  mainPagePath = '/main',
  pollPath = '/poll',
  homePath = '/',
  authPath = '/',
}

export enum HeaderPayloads {
  authButton = 'auth-button',
  menuBurger = 'menu-burger',
  logoutLink = 'logout-link',
}

export enum storageKeys {
  USER_DATA = 'userData',
}
