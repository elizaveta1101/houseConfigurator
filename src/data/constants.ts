export enum MenuLinkPaths {
  throughElementsPath = '/admin/throughs',
  projectFinishPath = '/admin/projects',
  projectInvestPath = '/admin/invests',
  housesFinishPath = '/admin/houses',
  infoPollPath = '/admin/info-poll',
  clientDataPath = '/admin/clients',
  adminsSitePath = '/admin/admins',
  profilePath = '/admin/profile',
  ordersPath = '/admin/orders',
  mainPagePath = '/admin/main',
  pollPath = '/admin/poll',
  homePath = '/admin/',
  authPath = '/admin/',
  clientSide = '/'
}

export enum HeaderPayloads {
  authButton = 'auth-button',
  menuBurger = 'menu-burger',
  logoutLink = 'logout-link',
}

export enum storageKeys {
  USER_DATA = 'userData',
}
